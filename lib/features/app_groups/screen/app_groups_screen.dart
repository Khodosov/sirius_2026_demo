import 'package:flutter/material.dart';

class AppGroupsScreen extends StatelessWidget {
  const AppGroupsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('iOS App Groups'),
      ),
      body: const Center(
        child: Text('TODO: iOS App Groups example'),
      ),
    );
  }
}
